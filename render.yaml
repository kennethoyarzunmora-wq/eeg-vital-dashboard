services:
  - type: web
    name: eeg-vital-dashboard-api
    runtime: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn server:app --timeout 300 --workers 1
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.9
